name: Molecule

on:
  push:
    branches:
      - '**'
  pull_request:
  workflow_dispatch:

jobs:

  molecule:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Run molecule
        run: make molecule
